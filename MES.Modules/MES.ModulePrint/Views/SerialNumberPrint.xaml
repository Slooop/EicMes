<UserControl x:Class="MES.ModulePrint.SerialNumberPrint"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!--内容区域-->
        <TextBlock Text="流程卡号：" VerticalAlignment="Center" HorizontalAlignment="Right" />
        <TextBox  Grid.Column="1" />
        <Button Grid.Column="2" Content="打印" Margin="5,0,0,0" />
        <DataGrid  Height="300" Grid.Row="1" Grid.Column="1" VerticalAlignment="Top" Margin="0,5,0,0">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="SN"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
    
    
    
</UserControl>
