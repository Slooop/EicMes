<UserControl x:Class="MES.ModuleHR.Views.UserChangeClass"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MES.ModuleHR.Views"
             mc:Ignorable="d"
             d:DesignHeight="880" d:DesignWidth="660">
    <Grid Background="{DynamicResource Brush4}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock VerticalAlignment="Center">工号</TextBlock>
            <TextBox Width="100" />
            <TextBox Width="100 " />
            <Button Content="导入" />
            <Button Content="导出" />
        </StackPanel>
        <DataGrid Grid.Row="1" />
    </Grid>
</UserControl>